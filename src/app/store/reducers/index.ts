import * as DefaultReducer from "./default.reducer";
import { AppState, metaReducers, ROOT_REDUCERS } from "./reducer";

export {
    AppState,
    metaReducers,
    ROOT_REDUCERS,
    DefaultReducer
}